/*
  dashboard

  No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

  The version of the OpenAPI document: 0.0

  Generated by OpenAPI Generator: https://openapi-generator.tech
*/

syntax = "proto3";

package ;

import public "models/any_type.proto";
import public "models/dashboard_field_color.proto";
import public "models/dashboard_thresholds_config.proto";
import public "models/todo_object_mapping.proto";

message DashboardPanelFieldConfigDefaults {

  // The display value for this field.  This supports template variables blank is auto
  string displayName = 103536240;

  // This can be used by data sources that return and explicit naming structure for values and labels When this property is configured, this value is used rather than the default naming strategy.
  string displayNameFromDS = 325606024;

  // Human readable field metadata
  string description = 113933319;

  // An explict path to the field in the datasource.  When the frame meta includes a path, This will default to `${frame.meta.path}/${field.name}  When defined, this value can be used as an identifier within the datasource scope, and may be used to update the results
  string path = 3433509;

  // True if data source can write a value to the path.  Auth/authz are supported separately
  bool writeable = 332208713;

  // True if data source field supports ad-hoc filters
  bool filterable = 478944048;

  // Numeric Options
  string unit = 3594628;

  // Significant digits (for display)
  float decimals = 28661571;

  float min = 108114;

  float max = 107876;

  // Convert input values into a display string  TODO this one corresponds to a complex type with generics on the typescript side. Ouch. Will either need special care, or we'll just need to accept a very loosely specified schema. It's very unlikely we'll be able to translate cue to typescript generics in the general case, though this particular one *may* be able to work.
  repeated TODO_OBJECT_MAPPING mappings = 194445669;

  DashboardThresholdsConfig thresholds = 128276121;

  DashboardFieldColor color = 94842723;

  // // The behavior when clicking on a result
  repeated AnyType links = 102977465;

  // Alternative to empty string
  string noValue = 490800179;

  // custom is specified by the PanelFieldConfig field in panel plugin schemas.
  TODO_OBJECT_MAPPING custom = 275346577;

}
